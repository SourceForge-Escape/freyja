
To compile and install this program you need the required
libaries and headers, read doc/FAQ for details.

To compile and install:

	sh autogen.sh
	make install

There is alao a total build Makefile in the root of this project.
The total build will attempt to build all project deps in order and
install them, but only use this if you're really lazy.
